import { Container, Box } from '@mui/material';

export const ProjectSummary = () => {
    return (
        <Container maxWidth="sm">
            <Box sx={{ mt: 4, mb: 4 }}>
                Hey there
            </Box>
        </Container>
    );
}